import { SearchorgPipe } from './privia/searchorg.pipe';

describe('SearchorgPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchorgPipe();
    expect(pipe).toBeTruthy();
  });
});
