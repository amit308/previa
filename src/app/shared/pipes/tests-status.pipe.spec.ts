import { TestsStatusPipe } from './tests-status.pipe';

describe('TestsStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TestsStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
