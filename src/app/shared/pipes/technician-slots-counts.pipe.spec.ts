import { TechnicianSlotsCountsPipe } from './technician-slots-counts.pipe';

describe('TechnicianSlotsCountsPipe', () => {
  it('create an instance', () => {
    const pipe = new TechnicianSlotsCountsPipe();
    expect(pipe).toBeTruthy();
  });
});
