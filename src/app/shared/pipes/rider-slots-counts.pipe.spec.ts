import { RiderSlotsCountsPipe } from './rider-slots-counts.pipe';

describe('RiderSlotsCountsPipe', () => {
  it('create an instance', () => {
    const pipe = new RiderSlotsCountsPipe();
    expect(pipe).toBeTruthy();
  });
});
