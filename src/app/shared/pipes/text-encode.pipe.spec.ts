import { TextEncodePipe } from './text-encode.pipe';

describe('TextEncodePipe', () => {
  it('create an instance', () => {
    const pipe = new TextEncodePipe();
    expect(pipe).toBeTruthy();
  });
});
