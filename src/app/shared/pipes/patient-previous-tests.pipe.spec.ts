import { PatientPreviousTestsPipe } from './patient-previous-tests.pipe';

describe('PatientPreviousTestsPipe', () => {
  it('create an instance', () => {
    const pipe = new PatientPreviousTestsPipe();
    expect(pipe).toBeTruthy();
  });
});
