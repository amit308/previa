import { AuthoritiesPipe } from './authorities.pipe';

describe('AuthoritiesPipe', () => {
  it('create an instance', () => {
    const pipe = new AuthoritiesPipe();
    expect(pipe).toBeTruthy();
  });
});
